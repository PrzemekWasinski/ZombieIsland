export class Drop {
    constructor(health, damage, ammo, image, position, tilePosition, pickedUp) {
        this.health = health;
        this.damage = damage;
        this.ammo = ammo;
        this.image = image;
        this.position = position;
        this.tilePosition = tilePosition;
        this.pickedUp = pickedUp;
    }
}